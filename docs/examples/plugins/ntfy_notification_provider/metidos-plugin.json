{
  "id": "ntfy_notification_provider",
  "name": "ntfy Notification Provider",
  "version": "1.0.0",
  "metidosApiVersion": "v1",
  "main": "./index.ts",
  "description": "Demonstrates a notification provider that forwards Metidos notifications to an ntfy topic.",
  "color": "#06b6d4",
  "permissions": ["notification:provider", "network:fetch"],
  "network": {
    "allow": ["https://ntfy.sh/**"]
  },
  "env": [
    {
      "key": "NTFY_TOPIC",
      "description": "Default ntfy topic. Prefer a private, unguessable topic name.",
      "required": false,
      "secret": true
    },
    {
      "key": "NTFY_TOKEN",
      "description": "Optional bearer token for protected ntfy topics.",
      "required": false,
      "secret": true
    }
  ],
  "settings": [
    {
      "key": "server_url",
      "label": "ntfy server URL",
      "description": "Base URL for the ntfy server. The default manifest allowlist permits only https://ntfy.sh/.",
      "kind": "url",
      "default": "https://ntfy.sh",
      "required": true
    },
    {
      "key": "default_topic",
      "label": "Default ntfy topic",
      "description": "Fallback topic when NTFY_TOPIC is not set. Use a private, unguessable topic.",
      "kind": "secret",
      "required": false
    }
  ],
  "notificationProviders": [
    {
      "id": "ntfy",
      "name": "ntfy",
      "description": "Sends Metidos notifications to an ntfy topic and returns provider receipts.",
      "timeoutMs": 10000
    }
  ],
  "limits": {
    "maxNetworkResponseBytes": 262144
  }
}
